Answers for the Interaction Lecture 1
L1_Q1_2018_2
The number 0 True

The number 1 False

The number 3 False

The number 6 False

exp.: Finally an empty list is passed to product as argument and it produces value zero. Then multiplication by zero produces zero value.

L1_Q3_2018_2
if x then "Good" else "Bad"   True

if x else "Bad" False

if x then "Good" False

if x == True then "Good" else "Bad"  True

exp.: There have to be both "then" and "else" alternatives. The outcome have to be some value.

L1_Q4_2018_2
x₁ False

x₂ True

x₃ False

exp.: Greeting has a parameter x(_2) and x refers to that parameter.  

L1_Q5_2018_2
Right False

Left True

exp.: operator is left associative (tree goes to left). 
L1_Q6_2018_2
((map f) x):xs false

(map f) x:xs false

map f (x:xs) true

map f x:xs false

exp.: Map takes a function as first argument and then this combination of functions takes list as an argument.

L1_Q7_2018_2
Function application (f x) false

Division (E₁ / E₂) false

Multiplication (E₁ * E₂) false

Syntax error true

exp.: atan2 takes two arguments and no when theres no parentheses more than two arguments is passed to that function. Compiler doesn't understand this.

